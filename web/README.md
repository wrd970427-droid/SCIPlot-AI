# Web Demo

**推荐（无需 Node.js / npm）：** 在仓库根目录运行 `python run.py`，浏览器打开 http://127.0.0.1:8000

页面文件：`web/static/index.html`，由 FastAPI 提供。

可选 Vite 流程仅在已安装 Node.js 时使用：

```bash
npm install
npm run dev
```
